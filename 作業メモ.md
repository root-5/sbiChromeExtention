## 前置詞（to, at）の扱いについて
クラス名: 基本的に「名詞」で表現します。Backend や ApiClient のように、「何であるか」を端的に示します。MessageToBackend というクラス名も間違いではありませんが、少し冗長に感じられることが多いです。

メソッド名: 「アクション」を示すため、postMessageToBackend() や fetchFromApi() のように前置詞をつけることで、データの流れる方向が明確になり、可読性が高まります。
